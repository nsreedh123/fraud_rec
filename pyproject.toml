[project]
name = "fraud-det"
version = "0.1.0"
description = "Fraud Detection Project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "torch",
    "matplotlib",
    "seaborn",
    "jupyterlab",
]
